


ToDo:

radio button to select wether the height and the weight in cm || foot || inch || kg || lbs || whatever

qpixmap to set the logo on every window

moderator vs user vs admin

line edit validator

Track users login and logout

Track users insertion and deletion then do all the queries at the end of the session

